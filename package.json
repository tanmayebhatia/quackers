{
  "name": "quackers",
  "version": "0.1.0",
  "description": "A little egg-duck that lives on your screen.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test 'test/**/*.test.js'",
    "package": "electron-packager . Quackers --platform=darwin --arch=arm64 --app-bundle-id=com.quackers.desktop --icon=assets/icon.icns --out=dist --overwrite && codesign --force --deep --sign - dist/Quackers-darwin-arm64/Quackers.app",
    "update-app": "bash tools/update-app.sh",
    "install-app": "npm run package && rm -rf ~/Applications/Quackers.app && cp -R dist/Quackers-darwin-arm64/Quackers.app ~/Applications/ && open ~/Applications/Quackers.app"
  },
  "license": "MIT",
  "devDependencies": {
    "@electron/packager": "^20.0.2",
    "electron": "^37.0.0"
  }
}
